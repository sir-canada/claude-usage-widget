# Screenshots

Add two PNGs here so the main README renders them:

- `panel.png` — the widget in the system tray / panel
- `popup.png` — the hover popup showing all limits

Capture with Spectacle (Rectangular Region, `Meta+Shift+PrintScreen`) or:

```sh
spectacle --region --background --nonotify --output screenshots/panel.png
```
